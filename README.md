# How to proxy local host to server.local.com, admin.local.com, etc...
```
Where server.local.com => backend
      admin.local.com  => frontend

httpd.conf lives in /etc/apache2/
hosts lives in /etc
```

